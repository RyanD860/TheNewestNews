import { StyleSheet } from "react-native";

const article = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    padding: 5
  }
});

export default article;
