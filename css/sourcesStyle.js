import { StyleSheet } from "react-native";

const source = StyleSheet.create({
  container: {
    marginTop: 30,
    flex: 2,
    alignItems: "center",
    flexWrap: "wrap"
  },
  item: {
    padding: 3
  }
});

export default source;
